/*
Suppose we want to calculate sum from i = 0 to i = n of a[i % m] - b[i % m]. It is good sometimes to look at the sum as
sum of a[i % m] - sum of b[i % m]s. expand the sum if needed and calculate how much contribution each value have to the answer.
*/
