/*
if numbers are posivie (numinator can be 0) then
*/
ceil(n / m) = (n + m - 1) / m;
// pitfalls : overflow and not considering the fact that numbers are positive
floor(n / m) = n / m
// pitfalls : not considering the fact that numbers are positive
